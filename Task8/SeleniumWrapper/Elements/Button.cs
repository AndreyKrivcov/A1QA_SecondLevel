using SeleniumWrapper.Browser;

namespace SeleniumWrapper.Elements
{
    public sealed class Button : BaseElement
    {
        public Button(WebElementKeeper elementKeeper) : base(elementKeeper)
        {
        }
    }
}