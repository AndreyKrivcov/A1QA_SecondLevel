using SeleniumWrapper.Browser;

namespace SeleniumWrapper.Elements
{
    public sealed class H2 : BaseElement
    {
        public H2(WebElementKeeper elementKeeper) : base(elementKeeper)
        {
        }
    }
}